{#
/**
 * @file dsi_collection.html.twig
 * Default theme implementation to present Collection data.
 *
 * This template is used when viewing Collection pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_dsi_collection()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('dsi_collection') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
